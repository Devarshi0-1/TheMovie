# Base URL of the TheMovie backend (Hono API + BetterAuth).
# Defaults to http://localhost:3100 (the local backend port) when unset.
# The dev server runs on :5173, which the backend trusts for CORS + auth
# (backend FRONTEND_URL=http://localhost:5173).
VITE_API_URL=http://localhost:3100
